
extends ../layouts/main.pug

block main
  section.section
    nav.navbar__section
      a(href="/") jackwui
      span /
      a(href="/") nodes
      span /
      b= node.name

    nav.navbar__node
      a(href=`/node/${node.name}/connections`) connections
      span |
      a sync
      span |
      a edit

    pre= JSON.stringify(node)


